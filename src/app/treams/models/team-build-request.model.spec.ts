import {TeamBuildRequest} from './team-build-request.model';

describe('TeamBuildRequest', () => {
  it('should create an instance', () => {
    expect(new TeamBuildRequest()).toBeTruthy();
  });
});
